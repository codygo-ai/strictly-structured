{
  "name": "@ssv/lambdas",
  "version": "0.0.0",
  "private": true,
  "description": "Lambda API for Structured Schema Validator (validate + optional compatibility)",
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsup src/handler.ts --format esm --target node18 --clean",
    "dev": "tsx watch src/dev-server.ts",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@google/generative-ai": "^0.21.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@ssv/tsconfig": "workspace:*",
    "@types/aws-lambda": "^8.10.145",
    "tsup": "^8.0.0",
    "typescript": "^5.7.2"
  }
}
