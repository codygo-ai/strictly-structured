# Base URL of the validate API (Lambda or gateway). Required for the Validate button.
# Example: https://abc123.lambda-url.us-east-1.on.aws
# Or: https://api.example.com
NEXT_PUBLIC_VALIDATE_API_URL=
