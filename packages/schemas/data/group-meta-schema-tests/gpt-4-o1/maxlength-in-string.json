{
  "_meta": {
    "groupId": "gpt-4-o1",
    "name": "maxLength in string",
    "description": "invalid: maxLength in string",
    "expectation": "invalid: error must match additional propert|maxLength",
    "expected": "invalid",
    "expectErrorPattern": "additional propert|maxLength"
  },
  "type": "object",
  "properties": {
    "s": {
      "type": "string",
      "maxLength": 100
    }
  },
  "additionalProperties": false,
  "required": [
    "s"
  ]
}