{
  "name": "@ssv/codegen",
  "version": "0.0.0",
  "private": true,
  "description": "Bidirectional Zod/Pydantic ↔ JSON Schema conversion and SDK transform simulation",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup && cp src/pydantic/runner.py dist/",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "json-schema-to-zod": "^2.7.0",
    "zod": "^3.24.0",
    "zod-to-json-schema": "^3.24.0"
  },
  "devDependencies": {
    "@ssv/schemas": "workspace:*",
    "@types/node": "^22.15.0",
    "eslint": "^9.39.1",
    "tsup": "^8.5.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22.15.0"
  }
}
