# Google OAuth 2.0 Web client ID for Google One Tap sign-in. Same client as Firebase Google sign-in (Firebase Console → Project settings, or Google Cloud Console → Credentials).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
