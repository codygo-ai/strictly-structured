{
  "name": "functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node18 --out-dir lib --clean --no-splitting --external firebase-functions --external firebase-admin --external openai --external @anthropic-ai/sdk --external @google/generative-ai",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "openai": "^4.73.0",
    "@anthropic-ai/sdk": "^0.32.1",
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@ssv/lambdas": "workspace:*",
    "@ssv/tsconfig": "workspace:*",
    "tsup": "^8.0.0",
    "typescript": "^5.7.2"
  }
}
